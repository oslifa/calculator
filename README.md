# calculator
A calculator which can perform and display addition, subtraction, multiplication, and division operations on integers and non-integers, producing results rounded to 10 decimal places.

The logic to calculate and display the operations was coded in JavaScript using functions, conditional statements, array and string methods, and an event listener.